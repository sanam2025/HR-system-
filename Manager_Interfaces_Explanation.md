# 📊 شرح واجهات قسم المدير (Manager Panel) في نظام Masar HR

يعتبر قسم المدير (Manager) من أهم الأقسام في نظام **Masar HR**، حيث يمتلك المدير صلاحيات إدارة فريقه، متابعة دوامهم، تقييم أدائهم، وطلب توظيف كوادر جديدة. 

إليك شرحاً تفصيلياً لكل ما يتعلق بواجهات المدير، والمكتبات المستخدمة، وكيفية بناء النظام:

---

## 1️⃣ نظرة عامة على واجهات المدير الرئيسية

يحتوي قسم المدير على الواجهات التالية (والموجودة ضمن المسار `/manager`):

1. **لوحة التحكم (Dashboard):** وهي الواجهة الرئيسية التي تلخص حالة القسم. تعرض إحصائيات سريعة عن عدد الموظفين، المهام المنجزة، أداء القسم، بالإضافة إلى مخططات بيانية توضح معدلات الحضور والتقييم الشهري.
2. **ملفي الشخصي (Profile):** واجهة مخصصة لعرض معلومات المدير نفسه، رصيد إجازاته، وتفاصيله الشخصية.
3. **فريق العمل (Employees):** قائمة تعرض جميع الموظفين التابعين لهذا المدير، مع إمكانية الدخول إلى الملف الشخصي لكل موظف (Employee Profile) لمشاهدة تفاصيل حضوره ومهامه.
4. **المهام (Tasks):** لوحة مهام (كانبان Kanban أو قائمة) تتيح للمدير إسناد مهام جديدة لموظفيه، ومتابعة حالة تنفيذها (قيد الانتظار، جارية، مكتملة).
5. **طلبات الإجازات والمغادرات (Leaves & Overtime):** شاشة تتيح للمدير استعراض طلبات الإجازات، المغادرات الساعية، والعمل الإضافي المقدمة من أعضاء فريقه، لاتخاذ قرار بالموافقة أو الرفض.
6. **سجل الحضور (Attendance):** واجهة لمتابعة أوقات دخول وخروج الموظفين بشكل يومي، وتحديد حالات التأخير والغياب.
7. **التقييم الدوري (Periodic Evaluation):** واجهة لتقييم أداء الموظفين بشكل دوري (شهري أو ربع سنوي) بناءً على مهامهم المنجزة والتزامهم.
8. **طلبات التوظيف (Recruitment):** (وسيتم شرحها بالتفصيل أدناه).
9. **المقابلات (Interviews):** لجدولة ومتابعة مقابلات العمل للمرشحين المحتملين للانضمام للقسم.
10. **التعاميم (Announcements):** لعرض التعاميم الإدارية الصادرة من الموارد البشرية.
11. **إنهاء الخدمة (Terminations):** لرفع طلبات الاستغناء أو إنهاء خدمات موظف معين في القسم لأسباب مبررة.

---

## 2️⃣ شرح واجهة "التوظيف / طلب احتياج وظيفي" (Recruitment)

**الهدف من الواجهة:**
تتيح هذه الواجهة للمدير تقديم طلب للموارد البشرية (HR) بأنه بحاجة لتوظيف شخص جديد في قسمه (Job Requisition).

**كيفية عمل الواجهة برمجياً:**
1. **النموذج (Form):** يوجد نموذج على يمين الشاشة يقوم المدير بتعبئته، ويشمل:
   - **المسمى الوظيفي (Job Title):** مثل "مهندس برمجيات".
   - **الوصف الوظيفي (Description):** المهام المطلوبة من الموظف.
   - **سنوات الخبرة (Experience):** بالسنوات.
   - **المهارات (Skills):** يمكن للمدير اختيار مهارات من قائمة منسدلة (مثل React, Node.js)، أو إضافة مهارة جديدة (Custom Skill) برمجياً عبر الضغط على زر "إضافة".
2. **قائمة الطلبات السابقة:** على يسار الشاشة، تظهر قائمة بالطلبات التي قدمها المدير مسبقاً وحالتها (معتمد، مرفوض، قيد النظر).
3. **العمليات المتاحة (CRUD):** يمكن للمدير **إنشاء** طلب جديد، **تعديل** طلب سابق لم يُعتمد بعد، أو **حذفه**.
4. **الحالة (Status Badge):** تتغير ألوان الحالة تلقائياً (أخضر للمعتمد، أحمر للمرفوض، أصفر للمُعلق).

---

## 3️⃣ المكتبات والتقنيات المستخدمة في بناء الواجهات

لضمان أداء عالٍ وتجربة مستخدم تفاعلية، تم استخدام مجموعة من أفضل المكتبات في عالم الـ **React**:

* **TanStack React Query (استرجاع البيانات):**
  - **الاستخدام:** تم استخدامها في واجهة التوظيف وغيرها لجلب البيانات (مثل `useQuery({ queryKey: ['my-job-requisitions'], queryFn: getJobRequisitions })`).
  - **الفائدة:** تقوم هذه المكتبة بحفظ البيانات مؤقتاً (Caching)، وتتولى إظهار حالات التحميل (Loading states) ومعالجة الأخطاء بسهولة، وتقوم بتحديث البيانات فوراً عند إضافة طلب جديد باستخدام `queryClient.invalidateQueries()`.
* **Zustand (إدارة الحالة):**
  - **الاستخدام:** تم استخدامها كبديل لـ Redux لسهولة التعامل معها (مثل `useAuthStore`).
  - **الفائدة:** حفظ بيانات المستخدم المسجل دخوله (المدير الحالى) وتمريرها بين الواجهات دون الحاجة لـ Prop Drilling.
* **Tailwind CSS (التصميم والاستايلنج):**
  - **الاستخدام:** تصميم كافة الواجهات باستخدام كلاسات Tailwind (مثل `bg-white rounded-2xl border flex items-center`).
  - **الفائدة:** سرعة كبيرة في التصميم، استجابة للشاشات المختلفة (Responsive)، وبناء واجهة احترافية بألوان مخصصة للموقع (مثل اللون الأخضر المميز).
* **Lucide-React (الأيقونات):**
  - **الاستخدام:** مكتبة أيقونات خفيفة ومفتوحة المصدر.
  - **الفائدة:** تجميل الواجهات (تم استخدام أيقونات مثل `Briefcase`, `ClipboardList`, `Trash2`, `Edit`).
* **React-Hot-Toast (الإشعارات المنبثقة):**
  - **الاستخدام:** إظهار رسائل النجاح أو الخطأ للمدير (مثلاً: "تم إرسال طلب الاحتياج بنجاح").

---

## 4️⃣ كيف تم رسم المخططات البيانية (Charts)؟

تم رسم المخططات البيانية الموجودة في لوحة التحكم (Dashboard) باستخدام مكتبة **`Recharts`**. وهي واحدة من أقوى المكتبات المبنية على React والمخصصة لرسم المخططات البيانية المتفاعلة.

**الآلية المستخدمة:**
1. **استقبال البيانات من الـ API:**
   يتم جلب البيانات (مثل إحصائيات حضور القسم أو تقييماتهم) من الـ Backend على شكل مصفوفة كائنات (Array of Objects).
2. **رسم المخطط الخطي (LineChart / BarChart):**
   - يتم تمرير المصفوفة إلى مكون `<ResponsiveContainer>` ليأخذ العرض الكامل للشاشة.
   - داخله يتم وضع مكون `<LineChart>` أو `<BarChart>` ويتم ربط محوره السيني (`<XAxis>`) بأسماء الأشهر أو الأيام.
   - المحور الصادي (`<YAxis>`) يمثل القيم (مثل نسبة التقييم أو عدد الموظفين).
3. **التفاعل والنوافذ المنبثقة (Tooltips):**
   - تم استخدام مكون `<Tooltip />` من مكتبة Recharts بحيث يظهر للمدير تفاصيل الرقم عند تمرير الماوس (Hover) فوق الخط البياني.
4. **التصميم:**
   - تم تغيير ألوان الخطوط البيانية (Stroke) لتتطابق مع هوية الموقع (اللون الأخضر)، وتم إضافة تدرجات لونية (Gradients) لجعل المخططات تبدو أنيقة واحترافية.
