# مهمة: تطوير نظام التعميمات (Announcements System)

## نظرة عامة
المطلوب بناء نظامين مترابطين:
1. واجهة عرض للموظفين (Employee/Manager/HR) تظهر التعميمات النشطة فقط.
2. واجهة إدارة كاملة (Admin/HR/Manager) لإنشاء وتعديل ونشر التعميمات.

---

## 1) واجهة عرض الإعلانات النشطة
- **المكان:** أعلى/ضمن الـ Dashboard الرئيسي، مرئية لكل الأدوار (HR, Manager, Employee).
- **Endpoint:** `GET /announcements/active`
- **السلوك:**
  - إذا وُجد تعميم نشط واحد أو أكثر → تُعرض البطاقات.
  - إذا لم يوجد أي تعميم نشط → القسم بأكمله يختفي (لا يظهر عنوان أو مساحة فارغة).
- **كل بطاقة تعرض:**
  - أيقونة/لون حسب الأولوية أو النوع (عاجل 🔴 / عادي 🟡 / إشعار عام 🟢).
  - العنوان.
  - نص التعميم.
  - التاريخ (starts_at أو تاريخ النشر).
- **فلترة الجمهور:** يجب أن يعرض فقط التعميمات المستهدفة للمستخدم الحالي (الكل / قسمه / دوره كمدير).

---

## 2) واجهة إدارة التعميمات (Admin/HR/Manager)
- **المكان:** لوحة التحكم → إدارة التعميمات.
- **Endpoint:** `GET /announcements` (يعرض كل الحالات: نشط، مجدول، منتهي، مسودة).
- **الأعمدة:** # | العنوان | الجمهور المستهدف | الحالة | الإجراءات (تعديل ✏️ / حذف 🗑️ / نشر فوري 📢).

### صلاحيات الإنشاء والاستهداف
| الدور | يقدر يحدد الجمهور؟ | الخيارات المتاحة |
|---|---|---|
| Admin | نعم | الكل / قسم معيّن (يختار من قائمة الأقسام) / المدراء فقط |
| HR | نعم | الكل / قسم معيّن (يختار من قائمة الأقسام) / المدراء فقط |
| Manager | لا (تلقائي) | التعميم يُنشر حصراً لقسمه هو فقط |

- عند اختيار "قسم معيّن" (Admin/HR): يجب أن تظهر قائمة منسدلة بالأقسام الموجودة فعلياً في النظام (من جدول Departments) لاختيار قسم واحد أو أكثر.
- الأدوار الثلاثة (Admin/HR/Manager) يملكون صلاحية: إنشاء (Create) / تعديل (Update) / حذف (Delete) على تعميماتهم أو حسب صلاحياتهم.

### حالات التعميم (Status)
- **مسودة (Draft):** محفوظ ولم يُنشر بعد.
- **مجدول (Scheduled):** له `starts_at` مستقبلي.
- **نشط (Active):** `starts_at <= now()` و `ends_at` (إن وجد) لم ينتهِ بعد.
- **منتهي (Expired):** تجاوز `ends_at`.

### ميزة "نشر الآن" (Publish Now)
- تظهر بجانب كل تعميم بحالة **مجدول** فقط.
- **دالة `GET`/endpoint لجلب التعميمات المجدولة** لكل مستخدم (حسب صلاحيته) لعرض زر "نشر الآن" بجانبها.
- عند الضغط على "نشر الآن"، تنفّذ الدالة `publish()`:
  - `starts_at = now()`
  - `status = active`
  - يظهر التعميم فوراً في واجهة الموظفين دون انتظار التاريخ الأصلي.

---

## المتطلبات التقنية المطلوب تحديدها من المطوّر
1. Data Model لجدول `announcements` يتضمن: id, title, body, audience_type (all/department/managers), department_id (nullable), created_by, role_of_creator, starts_at, ends_at (nullable), status, created_at, updated_at.
2. منطق فلترة الجمهور عند الجلب (خصوصاً حالة الـ Manager: يرى فقط تعميمات قسمه + التعميمات العامة).
3. Endpoints مطلوبة:
   - `GET /announcements/active` (للموظفين، مفلترة حسب الجمهور)
   - `GET /announcements` (للإدارة، كل الحالات)
   - `GET /announcements/scheduled` (لعرض زر Publish Now)
   - `POST /announcements` (إنشاء)
   - `PUT /announcements/:id` (تعديل)
   - `DELETE /announcements/:id` (حذف)
   - `PATCH /announcements/:id/publish` (نشر فوري)
4. Validation: لا يمكن لـ Manager اختيار جمهور غير قسمه.
5. UI: يجب إخفاء قسم "التعميمات" بالكامل من الـ Dashboard إذا كانت القائمة فارغة.

---

## المطلوب من المطوّر/الأداة
اقترح البنية الكاملة (Backend model + endpoints + منطق الصلاحيات) ثم نفّذ واجهتي العرض والإدارة المذكورتين أعلاه، مع الالتزام بمنطق الحالات (Draft/Scheduled/Active/Expired) وميزة النشر الفوري كما هو موضح.
